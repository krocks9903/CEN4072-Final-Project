<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">
<suite name="LinkedIn Test Suite" parallel="classes" thread-count="3" verbose="2">

    <!--
        PARALLEL TESTING CONFIGURATION:
        - parallel="classes"  → Each test class runs in its own thread
        - thread-count="3"    → Up to 3 classes run simultaneously (one per team member)
        - Each thread gets its own Firefox instance via ThreadLocal in BaseTest
    -->

    <!-- ============================================================ -->
    <!-- PUBLIC PAGE TESTS (No login required)                         -->
    <!-- ============================================================ -->
    <test name="Public Page Tests">
        <classes>
            <class name="tests.HomepageTest"/>
            <class name="tests.LoginPageTest"/>
        </classes>
    </test>

    <!-- ============================================================ -->
    <!-- AUTHENTICATED TESTS (Login required)                          -->
    <!-- ============================================================ -->
    <test name="Login and Authentication Tests">
        <classes>
            <class name="tests.LoginAuthTest"/>
        </classes>
    </test>

    <test name="Search and Navigation Tests">
        <classes>
            <class name="tests.SearchTest"/>
            <class name="tests.NavigationTest"/>
        </classes>
    </test>

    <test name="Jobs Page Tests">
        <classes>
            <class name="tests.JobsPageTest"/>
        </classes>
    </test>

    <test name="Profile and Social Tests">
        <classes>
            <class name="tests.ProfilePageTest"/>
            <class name="tests.MessagingTest"/>
            <class name="tests.NetworkTest"/>
        </classes>
    </test>

</suite>
